function square(num) {
    return num * num;
}

const square = (num) => {
    return num * num;
}

const square = num => num * num;

/*
Funciones flecha

La arrow functions consiste en una función anónima. Se denomina función flecha por el elemento "=>" en su sintaxis. 
En las arrow functions si existe un solo parámetro, se puede omitir los paréntesis. También tienen un retorno 
implícito, es decir, se puede omitir la palabra reservada return, para que el código sea escrito en una sola línea 
y si el retorno requiere de más líneas e igualmente se desea utilizar de manera implícita, se debe envolver el 
cuerpo de la función entre corchetes. Es una forma amigable para declarar una función y para este tipo de función se 
recomienda utilizar const.

Funciones flecha: https://developer.mozilla.org/es/docs/Web/JavaScript/Reference/Functions/Arrow_functions
*/
